import { Websocket } from './websocket'

import { test, it, expect, describe } from '@jest/globals'

//// Tests ////

describe('OnValidate', () => {

    test.todo('asserts unique; no other websocket module can exist in the tree')

    test.todo('asserts root parent; must be parented to the root')

})