import { feathers } from "../app-builder"

import { SocketIO } from './socketio'
import { MongoDb } from './mongo-db'
import { MongoDbService } from './mongo-db-service'

it(`registers a mongo db service`, () => {

    // class TodoService extends MongoDbService {

    //     readonly path = `todos` as const
    
    // }

    // const mongodbapp = feathers
    //     .add(SocketIO)
    //     .add(MongoDb)
    //     .add(TodoService)

})