/*** Main ***/

/**
 * Wraps the feathers resolver hook allowing it to use @benzed/schema instead
 * And then make sure renderer passes tests
 */
function resolver(): void {
    // DO MONDAY
}

/*** Exports ***/

export default resolver

export {
    
    resolver
}