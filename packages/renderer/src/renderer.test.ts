import { Renderer } from './renderer'
import { RendererOptions } from './render-options'

import os from 'os'

describe('configuration', () => {

    it.todo('is constructed with renderer options for output')

})