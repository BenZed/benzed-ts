{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // Build Options
        "build": false, // we'll never need to build test fiels
        "target": "esnext" // prevents jest tests from failing
    }
}