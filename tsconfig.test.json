{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // Build Options
        "target": "esnext", // prevents jest tests from failing,
        "isolatedModules": true,
        "types": [
            "node",
            "jest"
        ]
    }
}